# GitLab Allowed Projects Update

1. **Issue** => The `triage.gitlab.allowed-projects` still points to a non-existing repository: `order-payments/payment-service`.
2. **Fix** => Update `triage.gitlab.allowed-projects` to the **Delivery Hazards** repository: `enterprise/parcel-systems/applications/delivery-hazards`.
